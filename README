- Symlink .config to ~/.config
- Update .gitconfig for username etc.
- Update/create .gemrc, .npmrc etc.

- Need to PackerCompile, PackerSync
  - If there is an issue, rm -rf ~/.local/share/nvim and ~/.cache/nvim folders
  - Then recompile / install.

Additional Commands:
- `gr` - Find all occurences of item under cursor
- `gd` - Go to definition.
- 'prefix-x' - Kill the pane in the tmux session list.
- 'prefix-,' - rename the current pane
- 'prefix-c' - create new pane

Alfred Workflows:
- Github https://www.packal.org/workflow/github-repos
- Audio Switch - https://www.packal.org/workflow/audio-switch
- 1 Password - https://developer.1password.com/docs/cli/get-started/#install
- Dash - https://github.com/Kapeli/Dash-Alfred-Workflow


Additional Apps:
- Dash (v6)


